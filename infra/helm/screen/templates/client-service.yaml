apiVersion: v1
kind: Service
metadata:
  name: client-service
spec:
  type: NodePort
  ports:
    - port: {{ .Values.client.port }}
      targetPort: {{ .Values.client.port }}
      nodePort: {{ .Values.client.nodePort }}
  selector:
    app: client